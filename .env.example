# No environment variables are required to run this app.
#
# The dashboard is fully client-side: the dataset ships with the bundle and all
# statistics are computed in the browser. There are no API calls at runtime.
#
# PORT is the only variable the production server reads. Cloud Run sets it
# automatically; override it only for local testing.
# PORT=8080
